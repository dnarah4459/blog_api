import { Editor } from "@tinymce/tinymce-react";
import { Link } from "react-router-dom"

export default function Dashboard() {
  return (
    <div className="min-h-screen flex flex-col text-[18px] font-[650] p-3">
      This is an all in one platform to view blogs, read blogs, and comment on other people's blogs. 
    </div>
  );
}